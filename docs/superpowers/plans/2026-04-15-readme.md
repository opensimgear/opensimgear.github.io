# README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or
> superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace placeholder repository README with short, accurate project overview and core local development
instructions.

**Architecture:** Keep implementation narrow to one markdown file update. Reuse repository conventions already present
in `AGENTS.md` and `package.json`, and avoid adding duplicate process detail or long-form documentation.

**Tech Stack:** Markdown, Astro 6, Starlight, pnpm

---

### Task 1: Draft concise README content

**Files:**

- Modify: `README.md`
- Reference: `AGENTS.md`
- Reference: `package.json`

- [ ] **Step 1: Replace placeholder title-only README with concise project content**

````md
# OpenSimGear Website

Documentation website for OpenSimGear, built with Astro and Starlight.

## Requirements

- Node.js
- pnpm

## Getting Started

```bash
pnpm install
pnpm dev
```

## Commands

- `pnpm dev` - start local dev server
- `pnpm build` - run Astro checks and production build
- `pnpm preview` - preview production build locally

## Tech Stack

- Astro 6
- Starlight
- Svelte 5
- Tailwind CSS v4
````

- [ ] **Step 2: Check markdown for requested scope**

Run mental verification against spec:

- 1-2 sentence overview only
- minimal setup only
- core commands only
- no badges
- no extra contribution or structure sections

Expected: content remains under roughly 30 lines and is easy to scan.

### Task 2: Verify repository accuracy

**Files:**

- Verify: `README.md`
- Verify: `package.json`
- Verify: `AGENTS.md`

- [ ] **Step 1: Confirm commands match repository scripts and conventions**

Check these exact values:

```text
package.json scripts:
- dev -> astro dev
- build -> astro check && astro build
- preview -> astro preview

AGENTS.md conventions:
- use pnpm
```

Expected: README command list matches repository behavior.

- [ ] **Step 2: Confirm final README stays concise and accurate**

Acceptance checklist:

```text
- Title stays "OpenSimGear Website"
- README explains repository purpose
- README includes pnpm setup
- README includes only common commands
- README includes short stack summary
```

Expected: README is accurate, concise, and aligned with approved design.
